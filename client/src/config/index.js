const routes = {
  home: "/",
  // login: "/login",
  // register: "/register",
  postDetail: "/post/:id",
  message: "/message",
  profile: "/profile/:id",
  discover: "/discover",
  notify: "/notify",
  notfound: "*"
}

export default routes;