import React from 'react'

function FollowBtn() {
  return (
    <button
      className='editBtn btn btn-outline-info'
      onClick={() => (true)}
    >
      Edit Profile
    </button>
  )
}

export default FollowBtn;